# Optional override.
# Default production behavior uses same-origin /api.
# Set this only if the frontend must call a different API origin at build time.
# For Amplify + EC2, set this to your public HTTPS backend origin, for example https://api.yourdomain.com.
VITE_API_URL=/api